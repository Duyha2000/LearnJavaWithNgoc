package package2;

public class Parent {
    protected int protectedVal;

    protected void protectedMethod() {
        System.out.println("Xin chào");
    }
}

// Thêm, sửa, xóa chỉnh sửa

// add(), edit(), delete() -> CRUD

// addStudent(), addData(), addEmployee()

// abstract class

// add()
// Parent(): abstract add()

// Child(): add()